import Login from "@/components/Login";

export const metadata = {
  title: "Login | Lurnex",
  description: "Sign in to your Lurnex account to manage jobs or apply for new opportunities.",
};

export default function LoginPage() {
  return <Login />;
}
