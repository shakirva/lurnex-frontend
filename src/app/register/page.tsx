import Register from "@/components/Register";

export const metadata = {
  title: "Join Lurnex | Create Your Account",
  description: "Join Lurnex as a job seeker to find positions in the Gulf, or as an employer to reach top talent.",
};

export default function RegisterPage() {
  return <Register />;
}
