import PostJob from "@/components/PostJob";

export const metadata = {
  title: "Post a Job | TriaGull Employer Portal",
  description: "Post a new job vacancy and reach thousands of qualified candidates.",
};

export default function PostJobPage() {
  return <PostJob />;
}

